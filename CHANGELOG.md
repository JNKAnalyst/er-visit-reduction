# Changelog

## 2026-05-05

- Added `docs/methodology.md` describing inputs, formulas, and limitations.
- Added VBA source modules under `model/vba/` (`modConfig`, `modScenarios`, `modDashboard`).
- Added CSV input templates under `templates/`.
- Added `scripts/build_workbook.py` to generate the .xlsx skeleton from templates.
- Added `scripts/test_calculations.py` mirroring the VBA scenario math as a regression check.
- Added LICENSE (MIT) and `.gitignore`.
